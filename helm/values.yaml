postgresql:
  global:
    postgresql:
      auth:
        postgresPassword: "admin"
        database: "orders"
  fullnameOverride: "postgres"
  primary:
    persistence:
      mountPath: /var/lib/postgresql/data
      size: 2Gi
  readReplicas:
    persistence:
      enabled: false
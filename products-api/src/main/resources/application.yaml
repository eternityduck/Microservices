spring:
  datasource:
    hikari:
      initialization-fail-timeout: 10000
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
